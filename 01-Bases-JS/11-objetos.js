/**
 * Objetos
 */

let alumno = {
    nombre: "Benji",
    edad: 36,
    suscriptor: false,
    ciudad: "Tecamac"
}

let atributos = Object.keys(alumno);
console.log(atributos);

let valores = Object.values(alumno);
console.log(valores);